# Wails v3 build output (the build/ scaffold itself is committed).
bin/
# go-task local checksum/state cache.
.task/
node_modules
frontend/dist
.DS_Store
.cursor/
.remember/
.superpowers/
# Zensical docs build output + local docs venv
/site/
/.venv/
